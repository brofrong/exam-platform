- Роль преподавателя (`teacher`): доступ к программам, урокам, тестам, проверке, инвайтам и аналитике
- Админ-страницы пользователей и настроек OpenRouter (токен + модель) — только для `admin`
- ИИ-чат автора справа от редактора теории и вопроса теста; кнопка в заголовке «Редактор…»
- У блоков кода в чате — «Вставить» в курсор и «Заменить всё» (с подтверждением), без ручного копирования JSON
- Превью теории в редакторе активности (switch сверху)
- Превью текста вопроса в редакторе теста
