- В дереве программ под уроком видны активности; клик открывает редактор в правой панели
- Filetree программ живёт в layout-роуте и не перемонтируется при смене route
- Клик по папке в дереве больше не схлопывает узел (chevron отдельно) — без мигания при навигации
- ZeroProvider больше не пересоздаёт клиент при навигации (стабильный context) — дерево не мигает «Пока нет программ»
