/** Re-export attempt grading types for transitional imports. */
export {
	applyReviewResults,
	type GradedAnswer,
	type GradedAnswers,
	markAnswersPending,
	type StudentAnswer,
	type StudentAnswers,
} from "#/server/grading/grade-attempt";
